# Trivy misconfiguration suppressions for this repo (intentional, documented).

# AVD-DS-0002 — the worker images build on the shared worker-bridge base, which
# runs as root by design today (same as scopes-lambda and services-s-3): the
# runtime user is governed by the agent's worker pod securityContext, not by a
# USER directive here. Revisit when the bridge ships a non-root variant.
AVD-DS-0002
